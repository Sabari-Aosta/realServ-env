# Checkout the QA branch
git checkout qa

# Pull latest changes from the remote QA branch
git pull origin qa

# Merge changes from the development branch (assuming 'dev' is your development branch)
git merge origin/dev --no-ff --no-edit

# Push changes to the remote QA branch
git push origin qa
